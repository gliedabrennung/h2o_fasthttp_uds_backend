# FastHTTP + Caddy via UDS server

Высокопроизводительный бэкенд на Go с использованием **FastHTTP**, работающий за реверс-прокси **Caddy** через **Unix Domain Sockets (UDS)**.

## 🚀 Особенности архитектуры

* **FastHTTP**: Использование одного из самых быстрых HTTP-движков для Go.
* **Unix Domain Sockets (UDS)**: Общение между Caddy и Go происходит через файл сокета (`/tmp/fasthttp.sock`), что снижает накладные расходы на сетевой стек и повышает безопасность.
* **Caddy**: Автоматическое управление SSL (HTTPS), сжатие трафика (Zstd/Gzip) и поддержка HTTP/2 / HTTP/3.
* **Graceful Shutdown**: Корректная обработка сигналов завершения с очисткой файла сокета.

## 🛠 Требования

* **Go** 1.20+
* **Caddy** (установленный в системе)
* **Make**

## Запуск

```text
make dev
```

### Тестирование

```text
curl -vLk http://localhost
```


